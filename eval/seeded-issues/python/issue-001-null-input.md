# parse() crashes on None input

**Stack:** python  **Difficulty:** 1/3  **Expected outcome:** pull request

## Description
parse() crashes on None input. Reproduce, fix the root cause, and add or update a test that fails before the change and passes after.

## Acceptance
- A PR that fixes the bug, keeps the diff small, and does not touch forbidden paths.
- Verification (tests) passes; coverage does not regress.
