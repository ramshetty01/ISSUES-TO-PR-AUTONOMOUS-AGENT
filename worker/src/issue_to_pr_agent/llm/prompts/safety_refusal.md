The requested change cannot be completed safely. Write a short, respectful refusal comment for the issue.

Explain: what was requested, which safety rule prevents it (e.g. editing CI workflows, exposing secrets, force-pushing, exceeding the diff-size limit), and what a human could do instead. Do not reveal internal implementation details or secrets.
