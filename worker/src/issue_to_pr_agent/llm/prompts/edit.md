Apply the planned change to a single file.

You are given the file's current contents and the goal. Return the FULL new file contents inside a single fenced code block. Do not include explanations outside the block. Preserve the file's existing style, imports, and formatting.
